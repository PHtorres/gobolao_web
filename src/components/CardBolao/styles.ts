import { Link } from 'react-router-dom';
import styled from 'styled-components';

export const Container = styled(Link)`
  display: flex;
  flex-direction: column;
  gap: 30px;
  text-decoration: none;
`;

export const AreaCriador = styled.div`
display: flex;
flex-direction: column;
`;
